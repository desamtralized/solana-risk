# Solana RPC endpoint for local validator
NEXT_PUBLIC_SOLANA_RPC_URL=http://127.0.0.1:8899

# Program ID from Anchor.toml (this is the default local deployment address)
NEXT_PUBLIC_PROGRAM_ID=G4irLCSNHfxh2eCVpXowciffLtbnwGm1mGXU28afPsPJ

# Commitment level for Solana connection
NEXT_PUBLIC_COMMITMENT=confirmed

# Network (localnet, devnet, mainnet-beta)
NEXT_PUBLIC_NETWORK=localnet

# Optional: WebSocket endpoint for local validator (for subscription-based updates)
NEXT_PUBLIC_SOLANA_WEBSOCKET_URL=ws://127.0.0.1:8900 